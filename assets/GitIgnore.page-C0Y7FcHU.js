import{j as i}from"./index-CKl68G2i.js";import{C as e}from"./CommandLessonPageTemplate-DoAn-pAJ.js";import"./CommandBlock-D0TrVF3J.js";import"./SetupGuideHeader-C2CydxIK.js";import"./accordion-CoCJWeaM.js";const t=[{id:"start-with-rules",title:"เริ่มต้นเขียน .gitignore ให้ตรงกับโปรเจกต์",summary:"กำหนดไฟล์และโฟลเดอร์ที่ไม่ควรเข้า repository ตั้งแต่แรก",items:[{id:"create-ignore-file",title:"สร้าง .gitignore สำหรับไฟล์ที่ไม่ควรถูก track",purpose:"กัน build output, dependencies และ secret ไม่ให้หลุดเข้า commit history",commands:[{command:"touch .gitignore",description:"สร้างไฟล์ .gitignore ใน root ของ repository"},{command:'printf "node_modules/\\ndist/\\n.env\\n" > .gitignore',description:"เพิ่มรายการที่มักไม่ควร track ในโปรเจกต์ JavaScript หรือ Vite"},{command:"git status",description:"ตรวจว่ามีเฉพาะ .gitignore หรือไฟล์ที่ไม่ได้ถูก ignore เท่านั้นที่แสดงในผลลัพธ์"}],notes:["ใส่ / ต่อท้ายชื่อโฟลเดอร์เพื่อสื่อว่า ignore ทั้งโฟลเดอร์","ควรสร้าง .gitignore ตั้งแต่เริ่ม repo เพื่อกันไฟล์หลุดเข้า history"]},{id:"use-patterns",title:"ใช้ pattern เพื่อ ignore หลายไฟล์พร้อมกัน",purpose:"ลดงานเขียนกฎซ้ำเมื่อไฟล์มีรูปแบบชื่อคล้ายกัน",commands:[{command:'printf "\\n*.log\\ncoverage/\\n.DS_Store\\n" >> .gitignore',description:"เพิ่ม pattern สำหรับไฟล์ log, coverage reports และไฟล์ระบบของ macOS"},{command:"git check-ignore -v debug.log",description:"ตรวจว่าไฟล์ตัวอย่างถูก ignore ด้วย rule บรรทัดไหนใน .gitignore"}]}]},{id:"handle-tracked-files",title:"แก้กรณีไฟล์ถูก track ไปแล้ว",summary:".gitignore จะไม่หยุดติดตามไฟล์ที่เคยเข้า index มาก่อน ต้องลบออกจาก cache ก่อน",items:[{id:"remove-from-index",title:"หยุด track ไฟล์หรือโฟลเดอร์ที่เคย add ไปแล้ว",purpose:"เอาไฟล์ออกจาก staging/index โดยไม่ลบไฟล์จริงในเครื่อง เพื่อให้ .gitignore เริ่มมีผล",commands:[{command:"git rm --cached .env",description:"ลบ .env ออกจาก index แต่ยังเก็บไฟล์ไว้ในเครื่องสำหรับใช้งานต่อ"},{command:"git rm -r --cached dist",description:"ลบทั้งโฟลเดอร์ dist ออกจากการ track เพื่อให้ build output ไม่เข้า commit ถัดไป"},{command:"git status",description:"ตรวจผลว่าไฟล์ถูก mark เป็น deleted from index และพร้อม commit การเปลี่ยนแปลง"}],notes:["หลังรัน git rm --cached แล้วควร commit เพื่ออัปเดต history ให้ตรงกับกฎใหม่"]},{id:"local-only-rules",title:"ignore เฉพาะเครื่องตัวเองโดยไม่ commit rule",purpose:"ใช้กับไฟล์ local notes หรือ config เฉพาะเครื่องที่ไม่ควรบังคับทุกคนในทีม",commands:[{command:'printf "notes.txt\\n" >> .git/info/exclude',description:"เพิ่มกฎ ignore แบบ local-only ซึ่งจะไม่ถูก commit ขึ้น repository"},{command:"git check-ignore -v notes.txt",description:"เช็กว่าไฟล์ local ถูก ignore จาก .git/info/exclude เรียบร้อยแล้ว"}]}]}],o=[{id:"gitignore-lab",title:"สถานการณ์ฝึกใช้ .gitignore",summary:"ฝึกทั้งกรณีตั้งกฎตั้งแต่เริ่ม repo และกรณีแก้ไฟล์ที่เคยถูก track ไปแล้ว",labs:[{id:"starter-node-ignore",title:"Starter: กันไฟล์ build และ secret ตั้งแต่ต้น",summary:"ตั้ง .gitignore สำหรับโปรเจกต์ใหม่ แล้วตรวจว่า Git แสดงเฉพาะไฟล์ที่ควร commit",difficulty:"Starter",focus:"สร้างกฎ ignore พื้นฐาน",task:"สร้าง repo ทดลองและเพิ่มกฎให้ node_modules, dist และ .env ไม่โผล่ใน git status",commands:[{command:"mkdir gitignore-lab",description:"สร้างโฟลเดอร์ทดลองสำหรับฝึก .gitignore"},{command:"cd gitignore-lab",description:"เข้าโฟลเดอร์ทดลอง"},{command:"git init -b main",description:"เริ่ม repository ใหม่"},{command:'printf "node_modules/\\ndist/\\n.env\\n" > .gitignore',description:"สร้างกฎ ignore หลักที่เจอบ่อยในโปรเจกต์ web"},{command:"mkdir node_modules dist && touch .env README.md dist/app.js",description:"สร้างไฟล์และโฟลเดอร์ตัวอย่างเพื่อดูผลของ rule"},{command:"git status",description:"ตรวจว่าควรเห็น README.md และ .gitignore แต่ไม่ควรเห็น node_modules, dist, .env"}],checkpoint:"ผลลัพธ์ของ git status ต้องไม่แสดง node_modules/, dist/ และ .env เป็น untracked files"},{id:"practice-untrack-secret",title:"Practice: หยุด track ไฟล์ลับที่เคย add ไปแล้ว",summary:"ลองกรณีที่ .env เคยถูก stage หรือ commit มาก่อน แล้วต้องแก้ให้ .gitignore มีผลจริง",difficulty:"Practice",focus:"ลบไฟล์ออกจาก index แบบปลอดภัย",task:"จำลองว่ามี .env ที่ถูก track อยู่แล้ว จากนั้นทำให้ Git หยุดติดตามโดยไม่ลบไฟล์ในเครื่อง",commands:[{command:'echo "SECRET=demo" > .env',description:"สร้างไฟล์ลับตัวอย่าง"},{command:'git add .env && git commit -m "chore: add env for lab"',description:"จำลองสถานการณ์ที่ไฟล์ลับถูก track ไปแล้วในประวัติของ repo"},{command:'printf "\\n.env\\n" >> .gitignore',description:"เพิ่มกฎ ignore สำหรับ .env"},{command:"git rm --cached .env",description:"เอา .env ออกจาก index โดยเก็บไฟล์ไว้ในเครื่อง"},{command:"git status",description:"ตรวจว่าการเปลี่ยนแปลงพร้อมสำหรับ commit แก้ history tracking แล้ว"}],notes:["ถ้า commit ไม่ผ่านเพราะยังไม่ได้ตั้ง user.name หรือ user.email ให้ไปตั้งค่าก่อนตามบท Setup Git"],checkpoint:"หลัง git rm --cached .env แล้ว .env ต้องไม่ถูก track ต่อ และ commit ถัดไปควรมีแค่การลบออกจาก index"},{id:"challenge-local-ignore",title:"Challenge: แยกกฎส่วนกลางกับกฎเฉพาะเครื่อง",summary:"ฝึกเลือกใช้ .gitignore กับ .git/info/exclude ให้ตรงสถานการณ์ของทีมและของตัวเอง",difficulty:"Challenge",focus:"repo-wide vs local-only ignore",task:"กำหนดให้ build artifacts ถูก ignore ทั้งทีม แต่ไฟล์ notes ส่วนตัวถูก ignore แค่ในเครื่องเรา",commands:[{command:'printf "\\ncoverage/\\n" >> .gitignore',description:"เพิ่มกฎส่วนกลางสำหรับทุกคนในทีม"},{command:'printf "scratch-notes.txt\\n" >> .git/info/exclude',description:"เพิ่มกฎ ignore แบบ local-only สำหรับไฟล์โน้ตส่วนตัว"},{command:"git check-ignore -v coverage/report.txt",description:"เช็กว่า coverage ถูก ignore จาก .gitignore"},{command:"git check-ignore -v scratch-notes.txt",description:"เช็กว่าไฟล์โน้ตถูก ignore จาก .git/info/exclude"}],checkpoint:"coverage/ ต้องถูก ignore แบบแชร์ทั้งทีม ส่วน scratch-notes.txt ต้องถูก ignore เฉพาะเครื่องนี้เท่านั้น"}]}];function s(){return i.jsx(e,{badge:"Repository Basics",title:".gitignore: กันไฟล์ที่ไม่ควรเข้า repository ให้ถูกตั้งแต่ต้น",description:"เรียนรู้การเขียน .gitignore อย่างถูกหลัก, วิธีจัดการไฟล์ที่เคยถูก track ไปแล้ว และฝึกแยกกฎส่วนกลางของทีมออกจากกฎเฉพาะเครื่องตัวเอง",sections:t,labSections:o})}export{s as default};
